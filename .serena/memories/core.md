MoonBit `morning-start/prism`, preferred target wasm-gc. Architecture is Lux (vendor-neutral Lucent IR) -> provider adapters -> SDK/WASM façades. Public protocol contract is `docs/lux-ir-design.md`; any IR shape/evolution work must follow `docs/rules/lucent-ir-evolution.md`. Read `mem:tech_stack` for package boundaries, `mem:conventions` for adapter rules, and `mem:task_completion` for required verification.