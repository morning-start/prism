MoonBit blocks are separated with `///|`; preserve this block style. Lux IR must remain vendor-neutral, pure, and governed by explicit Exact/Degraded/Unsupported/Invalid outcomes—never silently drop nonportable semantics. Provider adapters implement the six bidirectional request/response/SSE conversions. Prefer stable assertion tests (`assert_eq`, `assert_true`); use `debug_inspect` + Debug for structured snapshots. Put deprecated blocks in package-local `deprecated.mbt`.