{
  "name": "prism-ts-wasm-example",
  "version": "0.1.0",
  "description": "Prism WASM TypeScript example - OpenAI Chat protocol conversion",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "start": "bun run src/main.ts",
    "test": "bun run src/test-wasm.ts",
    "test:env": "bun run src/test-env.ts",
    "test:e2e": "bun run src/test-e2e.ts",
    "test:all": "bun run src/test-wasm.ts && bun run src/test-env.ts && bun run src/test-e2e.ts"
  },
  "dependencies": {
    "@morning-start/prism-wasm": "file:../../wrappers/ts",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
